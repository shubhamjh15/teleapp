import { registerRootComponent } from 'expo';
import App from './App';

// registerRootComponent mounts the root component with
// React.StrictMode and ensures the web viewport fills 100% height.
registerRootComponent(App);
